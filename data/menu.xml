<?xml version='1.0' encoding='utf-8'?>
<odoo>
    
    <record id="action_update_form" model="ir.actions.server">
        <field name="name">Update SO</field>
        <field name="model_id" ref="sale.model_sale_order" />
        <field name="binding_model_id" ref="sale.model_sale_order" />
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">action = record.action_update_form()</field>
    </record>

    <record id="action_import_so" model="ir.actions.server">
        <field name="name">Import SO Product</field>
        <field name="model_id" ref="sale.model_sale_order" />
        <field name="binding_model_id" ref="sale.model_sale_order" />
        <field name="binding_view_types">form</field>
        <!-- <field name="binding_type">action</field> ======== -->
        <field name="state">code</field>
        <field name="code">action = record.action_import_so()</field>
    </record>
    
</odoo>